model City {
  id         BigInt   @id @default(autoincrement())
  name       String   @unique @db.VarChar(100)
  state      String   @db.VarChar(100)
  country    String   @db.Char(2) @default("IN")
  is_active  Boolean  @default(true)
  created_at DateTime?
  updated_at DateTime?
}
